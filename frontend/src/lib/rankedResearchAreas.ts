// Moved into @voteapp/api-client (mobile parity Phase 4) — the mobile issue
// editor needs the same row model and PUT conversion. Re-export keeps the
// web import sites.
export {
  INTEGRITY_ETHICS_SLUG,
  newRankedResearchArea,
  toPreferenceInputs,
  toRankedResearchAreas,
  type RankedResearchArea,
} from "@voteapp/api-client";
